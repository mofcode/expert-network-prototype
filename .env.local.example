# Anthropic API Key for Ask Expert AI feature (Server-side only)
# Get your API key from: https://console.anthropic.com/
# Note: This is NOT prefixed with NEXT_PUBLIC_ so it stays server-side only
ANTHROPIC_API_KEY=your-api-key-here
